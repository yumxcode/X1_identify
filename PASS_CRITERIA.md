# X1 参数辨识 — 通过标准（PASS CRITERIA）

> 版本 v1.0 · 2026-08-31
> 本文件先于任何交付物存在：所有 PASS/FAIL 声明必须引用本标准的条目编号与阈值。
> 实现方法：PRIME (arXiv:2605.17681) 的无 mocap 适配版（见 prime_identify/）。

## 0. 数据与验证通道

| 通道 | 数据/环境 | 用途 |
|---|---|---|
| A. 真机数据 | `x1_data/walk_diag_20260824_103222.csv`（15 s @ 100 Hz，12 关节 pos/vel/effort + IMU） | 辨识 + 留出集验证 |
| B. Sim GT 自测 | walk_diag 激励 + 已知参数扰动 + 噪声（σ_v=0.004 rad/s）合成 | 估计器正确性门禁 |
| C. gradmotion 远端 | gm 任务（git 仓库方式，ubuntu:22.04 + pip 依赖） | 独立复算 + 仿真验证 |

**传感边界（影响可达标准）**：本机无 mocap——基座线速度/位置不可观（论文全部实验依赖 OptiTrack）。基座平移不可观使部分参数子空间（尤基座惯量）信息量显著低于论文设定。标准按传感现实分级。

## 1. 门禁（Gate）条目——全部通过才允许声称"参数辨识"

### G1 估计器正确性（Sim GT 自测，通道 B）
- G1.1 合成数据扰动：base +2.0 kg / COM [0.02, 0, −0.03] m / I×1.2，**及** 左右 knee 各 +0.4 kg / COM [0.01, 0, 0] / I×1.1
- G1.2 质量恢复：每个扰动 body 恢复量 ≥ 50% 且绝对误差 ≤ 0.5 kg
- G1.3 COM 恢复误差 ≤ 20 mm（扰动 body）
- G1.4 惯量增量方向余弦 ≥ 0.7（扰动 body，若该方向可激励）
- **G1 未通过时：不得生成/更新任何"辨识后模型"交付物，不得进入真机结论**

### G2 目标一致性（真机，通道 A）
- G2.1 辨识器优化的目标与验收指标必须是同一个量（禁止：优化投影残差、验收全行残差）
- G2.2 验收指标：接触一致的力矩残差 `J(θ) = mean_k Σ_rows [Y p − B u − Jᵀλ_k/dt]²`（trusted rows；λ 由当前 θ 的接触 QP 重解，与 `run_real_walk.fie_cost` 相同定义）
- G2.3 PASS 条件：`J(θ_id) / J(θ_nom) ≤ 0.95`（train），且 holdout 不恶化超过 +5%
- G2.4 迭代过程必须单调或带步长线搜索回退（禁止发散链输出）

### G3 物理一致性
- G3.1 所有辨识 body 的伪惯量 ℐ ≻ 0（log-Cholesky 参数化自动保证；交付时逐 body 断言）
- G3.2 总质量 > 0 且与 GRF 平衡估计（G4）偏差 ≤ 2%

### G4 整机质量（GRF 平衡，通道 A）
- 指标：mean(Σλⁿ)/g over 收敛帧（接触 QP 的竖直力平衡）
- 报告形式：点估计 + 分段 bootstrap 95% CI（≥20 段）
- 交叉验证（可得时）：整机称重；否则报告与 URDF 名义的偏差及可能来源（外接件/电池）
- 该条目**独立于 G1-G2**：不因估计器未达标而失效，但只能声称"质量估计"，不得声称"参数辨识"

### G5 gradmotion 远端验证（通道 C）
- G5.1 远端任务可拉取仓库并运行验证脚本（流程打通）
- G5.2 远端独立复算 G4 的质量估计，与本地偏差 ≤ 0.2 kg
- G5.3 若 G1-G2 通过：远端加载辨识模型做仿真对比（GRF 轨迹、关节力矩预测的相对误差 ≤ 名义模型）
- G5.4 任务日志/笔记留档（gm task note）

## 2. 交付物清单（按 G 条目授权）

| 交付物 | 前置门禁 | 当前状态 |
|---|---|---|
| `results/x1_gmass_repair.urdf`（物理修复+质量锚定，非辨识输出） | G3.1, G4 | 已生成（命名已如实标注 repair） |
| `results/x1_identified.urdf`（真辨识输出） | G1+G2+G3 | **禁止生成直至 G1/G2 通过** |
| `results/real_walk_ident.json` | G2 | 已有（当前 FAIL：残差恶化，见 §3） |
| 估计器自测报告 | G1 | 待通过 |
| 远端验证任务记录 | G5 | 进行中 |

## 3. 当前状态（2026-08-31 终版，诚实记录）

- **G1 FAIL**：sim 自测含腿组扰动，恢复 ~0-3%。根因不是实现缺陷而是**可辨识性物理边界**——参数信号被刚性接触的 λ 重解抵消（证据链见 `prime_identify/IDENTIFIABILITY.md`：信号通道开放 [GT 成本 9.7 vs nominal 67.7] 但标量扫描平坦 [82.9-85.9 无谷]、null(J) 投影后扰动方向近零、swing 行经基座加速度耦合 λ 通道）。论文靠 mocap 基座测量打破抵消，本数据无此传感。
- **G2 FAIL**：真机回归 train 33283→200142（+501%），holdout +532%。与 G1 同根因。按 §4.3 路径收敛。
- **G3 PASS**：URDF 名义参数物理不一致（13/13 body 伪惯量负特征值，SolidWorks 导出缺陷）已发现并修复；投影后全 body 一致；round-trip 1e-14。交付 `results/x1_gmass_repair.urdf`（命名如实标注 repair，非辨识输出）。
- **G4 PASS**：GRF 质量估计 37.10 kg（+1.78 kg vs URDF 35.32，+5.0%）；竖直力平衡静态合成测试精度 0.1%；CI 由远端复算补充。
- **G5 进行中**：远端任务已创建（TASK_20260831_071 失败无日志，hello 连通性任务 TASK_20260831_104 排队中）。
- 估计器基建（全部有单测/自测背书）：log-Cholesky 往返 4e-16；参数管线 round-trip 1.07e-14；接触 QP 力平衡 0.1%；GRF/水平比 0.999。

## 4. 修复路线（按优先级）

1. G2.1 修正：估计器步长线搜索（对齐优化目标与验收指标）；重评真机 before/after
2. G1 重跑：含腿组扰动的完整自测
3. 若 1-2 失败：明确结论"当前传感配置下 PRIME 级参数辨识不可达"，交付 G3/G4 成果 + 可辨识性分析（论文对照：其全部实验有 mocap）
4. 任何情况下：G5 远端通道完成
